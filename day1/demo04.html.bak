<html>
<head>
<title>字符与数字比较大小</title>
<meta http-equiv="content-type" content = "text/html;charset=utf-8"/>

<script language = "javascript">
	
	var a = 65;
	var b = "65" ;
	/*
	if(a == b){
		window.alert("ok!") 
	}else{
		window.alert("no!")
	}
	*/
	/*
	var val = window.prompt("请输入值：") ;
	//这里的val是一个字符串，不是一个数字，不能直接进行加减乘除
	//可以进行一次转换使用parseInt() or parseFloat()
	document.write(typeof(val)+'<br/>') ;
	//val只需要定义一次就可以了，也可以不定义，但是必须要赋值
	val = parseInt(val) ;
	document.write(typeof(val)+"<br/>") ;
	document.write("你的输入是："+val) ;
	*/
	var val1 = window.prompt("请输入一个值：") ;
	var val2 = window.prompt("请在输入一个数：");
	val1 = parseFloat(val1) ;
	val2 = parseFloat(val2) ;

	var result = val1+val2 ;
	document.write(result) ;
</script>
</head>
</html>